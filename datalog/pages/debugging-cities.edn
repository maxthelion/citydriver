[;; === debugging-cities.md ===

 ;; Pass 1 — Concepts
 [:db/add -1 :concept/name "Debugging Cities"]
 [:db/add -1 :concept/summary "How to inspect city generation by controlling archetype, tick, and bitmap lens via URL, including side-by-side comparison."]
 [:db/add -1 :concept/source-page "debugging-cities.md"]

 ;; Pass 2 — Relationships
 [:db/add -2 :rel/source -1]
 [:db/add -2 :rel/target "City Archetypes"]
 [:db/add -2 :rel/type :rel/depends-on]

 [:db/add -3 :rel/source -1]
 [:db/add -3 :rel/target "Pipeline Observability"]
 [:db/add -3 :rel/type :rel/depends-on]

 ;; Pass 3 — Decisions
 [:db/add -4 :decision/summary "All view state encoded in URL for linking and sharing specific debug views."]
 [:db/add -4 :decision/rationale "Enables reproducible debugging: link directly to tick 5 of market town viewed through land reservation lens."]
 [:db/add -4 :decision/affects -1]

 ;; Pass 4 — Invariants
 [:db/add -5 :invariant/statement "The current debug view is fully described by the URL (archetype, tick, bitmap lens)."]
 [:db/add -5 :invariant/confidence :high]
 [:db/add -5 :invariant/source-page "debugging-cities.md"]]
